# Server
PORT=4000

# MongoDB (use auth if your server requires it)
# No auth:  mongodb://localhost:27017/discharge-summary-db
# With auth: mongodb://USERNAME:PASSWORD@localhost:27017/discharge-summary-db
#           (escape special chars in PASSWORD, e.g. @ → %40)
MONGO_URI=mongodb://localhost:27017/discharge-summary-db

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Optional: Gemini AI (for AI-enhanced discharge summaries)
# GEMINI_API_KEY=

# Optional: WhatsApp (for future integrations)
# WHATSAPP_API_KEY=
